# Set USE_OPENAI=true and provide a key to run example/test.ts against real OpenAI.
# Default (no env vars): example/test.ts uses a mock model — no API key needed.
USE_OPENAI=false
OPENAI_API_KEY=
